@using Vendor.Models;
@using Vendor.Controllers;

<!DOCTYPE html>
<html>
    <head>
        <meta charset='utf-8'>
        <title>Vendor</title>
        <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
    </head>
    <body>
<h3>Vendor</h3>
<ol>
    @foreach(var item in @Model["items"])
    {
        <li><a href='/categories/@Model["category"].Id/items/@item.Id'>@item.getDescription()</a></li>
    }
</ol>

<p><a href='/categories/@Model["category"].Id/items/new'>Add New Item</a></p>
<p><a href='/categories'>Return to Vendor</a></p>
<p><a href='/'>Return to Homepage</a></p>
</body>
</html>
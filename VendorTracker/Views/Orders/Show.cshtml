@using VendorTracker.Models;
@* @using Vendor.Controllers; *@

<!DOCTYPE html>
<html>
    <head>
        <meta charset='utf-8'>
        <title>Find Order</title>
        <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
    </head>
    <body>
        <h1>Vendor</h1>

    <h2>@Model["order"].getDescription()</h2>
    <h3>Vendor: @Model["vendor"].getName()</h3>

    <a href='/vendors/@Model["vendor"].Id/orders/new'>Add Order</a>
    <a href='/vendors'>View</a>

</body>
</html>   